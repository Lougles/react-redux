import { createSlice } from "@reduxjs/toolkit";


const studentsReducer = createSlice({
  name: 'students',
  initialState: {
    students: [],
  },
  reducers: {
    addStudent: (state, action) => {
      
    }
  }
})